# Local dev (split Vite + API)
VITE_API_URL=http://localhost:5000/api
VITE_SERVER_URL=http://localhost:5000

# Docker / same-origin production build: use /api and omit VITE_SERVER_URL so the app uses the page origin.
